{
  "name": "friend-service",
  "version": "1.0.0",
  "description": "Simple Express + Prisma service with NeonDB (Postgres) for users and friends",
  "main": "src/index.js",
  "scripts": {
    "dev": "NODE_ENV=development nodemon --watch src --exec node src/index.js",
    "start": "nodemon src/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:studio": "prisma studio"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "prisma": "^5.0.0"
  }
}
